# We don't want to use the normal database for the app when running selenium tests,
# so override value of the default database

MONGO_DEFAULT_DB=selenium_app

GITHUB_AUTH_CLIENT_ID=selenium-github-client
GITHUB_AUTH_CLIENT_SECRET=selenium-github-secret

FLASK_LOGIN_DISABLED=True
FLASK_SECRET_KEY=dd597289-32cc-4cce-b07f-c03b0b286115